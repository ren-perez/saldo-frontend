// Re-export from shared — kept for backward compatibility
export type { FlowType, NodeKind, FlowTransaction, FlowCategory, FlowGroup, FlowRow, FlowHierarchyNode, SankeyNode, SankeyLink } from "./shared/types"
export { FLOW_TYPES, INCOME_COLOR, UNALLOCATED_COLOR } from "./shared/flow-colors"
